Ayumu Desktop Bridge settings:

- `port`: localhost port used by the bridge server
- `session_ttl_seconds`: lifetime for browser bridge sessions
- `allowed_origins`: Ayumu web origins allowed to read bridge data

If you run Ayumu from a different origin, add it to `allowed_origins` and restart Anki.
